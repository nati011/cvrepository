# Go API & worker use YAML config. Copy config.example.yaml to config.yaml
# (or set CONFIG_PATH to another file). Optional override:
#   go run ./cmd/api -config /path/to/config.yaml

# Next.js (web) — server-side calls to Go API (BFF)
# web/.env.local
API_URL=http://localhost:8080
